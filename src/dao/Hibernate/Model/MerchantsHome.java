package dao.Hibernate.Model;

// Generated 7 avr. 2010 17:51:20 by Hibernate Tools 3.2.2.GA

import java.util.List;
import javax.naming.InitialContext;
import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.hibernate.LockMode;
import org.hibernate.SessionFactory;
import static org.hibernate.criterion.Example.create;

/**
 * Home object for domain model class Merchants.
 * @see hiber.Merchants
 * @author Hibernate Tools
 */
public class MerchantsHome {

	private static final Log log = LogFactory.getLog(MerchantsHome.class);

	private final SessionFactory sessionFactory = getSessionFactory();

	protected SessionFactory getSessionFactory() {
		try {
			return (SessionFactory) new InitialContext()
					.lookup("SessionFactory");
		} catch (Exception e) {
			log.error("Could not locate SessionFactory in JNDI", e);
			throw new IllegalStateException(
					"Could not locate SessionFactory in JNDI");
		}
	}

	public void persist(Merchants transientInstance) {
		log.debug("persisting Merchants instance");
		try {
			sessionFactory.getCurrentSession().persist(transientInstance);
			log.debug("persist successful");
		} catch (RuntimeException re) {
			log.error("persist failed", re);
			throw re;
		}
	}

	public void attachDirty(Merchants instance) {
		log.debug("attaching dirty Merchants instance");
		try {
			sessionFactory.getCurrentSession().saveOrUpdate(instance);
			log.debug("attach successful");
		} catch (RuntimeException re) {
			log.error("attach failed", re);
			throw re;
		}
	}

	public void attachClean(Merchants instance) {
		log.debug("attaching clean Merchants instance");
		try {
			sessionFactory.getCurrentSession().lock(instance, LockMode.NONE);
			log.debug("attach successful");
		} catch (RuntimeException re) {
			log.error("attach failed", re);
			throw re;
		}
	}

	public void delete(Merchants persistentInstance) {
		log.debug("deleting Merchants instance");
		try {
			sessionFactory.getCurrentSession().delete(persistentInstance);
			log.debug("delete successful");
		} catch (RuntimeException re) {
			log.error("delete failed", re);
			throw re;
		}
	}

	public Merchants merge(Merchants detachedInstance) {
		log.debug("merging Merchants instance");
		try {
			Merchants result = (Merchants) sessionFactory.getCurrentSession()
					.merge(detachedInstance);
			log.debug("merge successful");
			return result;
		} catch (RuntimeException re) {
			log.error("merge failed", re);
			throw re;
		}
	}

	public Merchants findById(java.lang.String id) {
		log.debug("getting Merchants instance with id: " + id);
		try {
			Merchants instance = (Merchants) sessionFactory.getCurrentSession()
					.get("hiber.Merchants", id);
			if (instance == null) {
				log.debug("get successful, no instance found");
			} else {
				log.debug("get successful, instance found");
			}
			return instance;
		} catch (RuntimeException re) {
			log.error("get failed", re);
			throw re;
		}
	}

	public List<Merchants> findByExample(Merchants instance) {
		log.debug("finding Merchants instance by example");
		try {
			List<Merchants> results = (List<Merchants>) sessionFactory
					.getCurrentSession().createCriteria("dao.Hibernate.Model.Merchants").add(
							create(instance)).list();
			log.debug("find by example successful, result size: "
					+ results.size());
			return results;
		} catch (RuntimeException re) {
			log.error("find by example failed", re);
			throw re;
		}
	}
}
